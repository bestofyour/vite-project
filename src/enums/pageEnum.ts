export enum PageEnum {
  // basic login path
  BASE_LOGIN = '/login',
  // basic home path
  BASE_HOME = '/dashboard',
  // error page path
  ERROR_PAGE = '/error',
  // error log page path
  ERROR_LOG_PAGE = '/error-log/list',
}

export enum ToolbarTypeEnum {
  ZOOM_IN = 'zoomIn',
  ZOOM_OUT = 'zoomOut',
  RESET_ZOOM = 'resetZoom',

  UNDO = 'undo',
  REDO = 'redo',

  SNAPSHOT = 'snapshot',
  VIEW_DATA = 'viewData',
}
